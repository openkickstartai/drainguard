{
  "name": "drainguard",
  "version": "0.1.0",
  "description": "Container graceful shutdown compliance validator — verify SIGTERM handling, exec form, preStop hooks, and readiness probes in CI",
  "bin": {
    "drainguard": "./src/index.ts"
  },
  "scripts": {
    "drainguard": "tsx src/index.ts",
    "test": "vitest run"
  },
  "keywords": ["kubernetes", "docker", "graceful-shutdown", "sigterm", "ci", "container"],
  "license": "MIT",
  "dependencies": {
    "js-yaml": "^4.1.0"
  },
  "devDependencies": {
    "@types/js-yaml": "^4.0.9",
    "@types/node": "^20.11.0",
    "tsx": "^4.7.0",
    "typescript": "^5.3.0",
    "vitest": "^1.6.0"
  }
}
